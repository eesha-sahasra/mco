apiVersion: v1
kind: Service
metadata:
  name: vs
spec:
  selector:
    app: vs
  ports:
    - name: http
      protocol: TCP
      port: 5006
      targetPort: 5006
  #externalIPs: 
    #- 192.168.1.22    
  type: LoadBalancer
